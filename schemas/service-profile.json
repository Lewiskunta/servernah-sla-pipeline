{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://servernah.atlancis.internal/schemas/service-profile/v1",
  "title": "ServiceProfile",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "type": "string",
      "const": "resilience/v1"
    },
    "kind": {
      "type": "string",
      "const": "Service"
    },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["service_id", "name", "department", "owner_team"],
      "properties": {
        "service_id": {
          "type": "string",
          "pattern": "^[a-z0-9-]{3,64}$",
          "description": "Primary key used as relational reference across all catalog files"
        },
        "name": {
          "type": "string",
          "minLength": 3,
          "maxLength": 128
        },
        "department": {
          "type": "string",
          "enum": ["devops", "ebu", "shared"]
        },
        "owner_team": {
          "type": "string",
          "minLength": 2,
          "maxLength": 64
        },
        "description": {
          "type": "string",
          "maxLength": 512
        },
        "crd_compatible": {
          "type": "boolean"
        }
      }
    },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tier", "topology", "sla_targets", "revenue_impact", "blast_radius"],
      "properties": {
        "tier": {
          "type": "string",
          "enum": [
            "tier-1-mission-critical",
            "tier-2-business-critical",
            "tier-3-business-operational",
            "tier-4-non-critical"
          ]
        },
        "topology": {
          "type": "object",
          "additionalProperties": false,
          "required": ["primary_zone", "failover_zone", "replication_mode"],
          "properties": {
            "primary_zone": {
              "type": "string",
              "enum": ["adc-primary", "paix-failover"]
            },
            "failover_zone": {
              "type": "string",
              "enum": ["adc-primary", "paix-failover"]
            },
            "replication_mode": {
              "type": "string",
              "enum": ["active-active", "active-passive", "passive-only"]
            }
          }
        },
        "sla_targets": {
          "type": "object",
          "additionalProperties": false,
          "required": ["rto_minutes", "rpo_minutes", "uptime_percent", "mttr_minutes"],
          "properties": {
            "rto_minutes": {
              "type": "integer",
              "minimum": 1,
              "maximum": 1440,
              "description": "Maximum acceptable time to restore service after outage"
            },
            "rpo_minutes": {
              "type": "integer",
              "minimum": 1,
              "maximum": 1440,
              "description": "Maximum acceptable data loss measured in time"
            },
            "uptime_percent": {
              "type": "number",
              "minimum": 95.0,
              "maximum": 100.0,
              "description": "Committed service uptime as a percentage"
            },
            "mttr_minutes": {
              "type": "integer",
              "minimum": 1,
              "maximum": 1440,
              "description": "Target mean time to recovery"
            }
          }
        },
        "revenue_impact": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low"]
        },
        "blast_radius": {
          "type": "object",
          "additionalProperties": false,
          "required": ["affected_customer_segments", "estimated_revenue_impact_usd_per_hour", "downstream_services_count", "regulatory_exposure"],
          "properties": {
            "affected_customer_segments": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": ["iaas-tenants", "baremetal-tenants", "paas-tenants", "internal-ops", "external-api-consumers"]
              },
              "minItems": 1
            },
            "estimated_revenue_impact_usd_per_hour": {
              "type": "integer",
              "minimum": 0
            },
            "downstream_services_count": {
              "type": "integer",
              "minimum": 0
            },
            "regulatory_exposure": {
              "type": "string",
              "enum": ["high", "medium", "low", "none"]
            }
          }
        }
      }
    }
  }
}